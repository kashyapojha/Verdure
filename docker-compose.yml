services:
  db:
    image: ${DOCKERHUB_USERNAME:-karann24}/verdure-db:latest
    build:
      context: ./DB
      dockerfile: Dockerfile
    container_name: verdure-db
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - verdure-network

  backend:
    image: ${DOCKERHUB_USERNAME:-karann24}/verdure-backend:latest
    build:
      context: ./Backend
      dockerfile: Dockerfile
    container_name: verdure-backend
    environment:
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      PREDICTOR_URL: http://ai_chatbot:5000/chat
    ports:
      - "3030:3030"
    depends_on:
      db:
        condition: service_healthy
      ai_chatbot:
        condition: service_started
    restart: unless-stopped
    networks:
      - verdure-network

  ai_chatbot:
    image: ${DOCKERHUB_USERNAME:-karann24}/verdure-ai-chatbot:latest
    build:
      context: ./AI-Chatbot
      dockerfile: Dockerfile
    container_name: verdure-ai-chatbot
    environment:
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      PREDICTOR_PORT: 5000
      OMP_NUM_THREADS: "1"
      TOKENIZERS_PARALLELISM: "false"
    ports:
      - "5000:5000"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; r=urllib.request.urlopen('http://127.0.0.1:5000/health', timeout=5); exit(0 if r.status==200 else 1)"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - verdure-network

  frontend:
    image: ${DOCKERHUB_USERNAME:-karann24}/verdure-frontend:latest
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    container_name: verdure-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - verdure-network

volumes:
  db_data:

networks:
  verdure-network:
    driver: bridge